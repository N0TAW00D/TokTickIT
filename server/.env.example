DATABASE_URL="postgresql://postgres:password@localhost:5432/localdb"

# Origin the client dev server runs on (Lab 3 session cookies, api-spec.md
# §1.2). Defaults to Vite's own default port when unset — override only if
# the client runs somewhere else.
CLIENT_ORIGIN="http://localhost:5173"
